---
name: DataDog Service Catalog
on:
  push:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Say "Hello"
        run: |
          echo "Hello, world!"
          pwd
          ls -ltr